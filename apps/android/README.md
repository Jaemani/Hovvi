# Hovvi Android

Android follows after the iOS attach path stabilizes.

The Android client should use Kotlin/Compose and share protocol concepts with the iOS client. Terminal attach, mosh-compatible transport, and tmux scrollback should stay in native or shared compiled code rather than a pure cross-platform UI layer.
